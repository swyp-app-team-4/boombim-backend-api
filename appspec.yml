version: 0.0
os: linux

files:
  - source: docker-compose.yml
    destination: /home/ec2-user/boombim

hooks:
  ApplicationStop:
    - location: scripts/stop.sh
      timeout: 60
      runas: ec2-user

  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 300
      runas: ec2-user
